<div
  style="height: {{ $height }}; width: {{ $width }};"
  class="js-advanced-google-maps"
  data-google-styles="{{ $googleStyles }}"
  data-mapbox-styles="{{ $mapboxStyles }}"
  data-zoom="{{ $zoom }}"
  data-address="{{ $address }}"
  data-mapbox-api-key="{{ $mapbox_api_key }}"
  data-map-type="{{ $mapType }}"
>
</div>
